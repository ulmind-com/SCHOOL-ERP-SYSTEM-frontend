'use client'

import { ResourceScreen } from '@/components/resource/resource-screen'
import { DOCUMENTS } from '@/components/resource/defs-more'

export default function Screen() {
  return <ResourceScreen def={DOCUMENTS} />
}
